const seedDemo = require("../seeds/demoSeeder");

// This script serves as a legacy wrapper for the new modular seeding system.
// It can be called via `npm run seed:demo`.

console.log("🚀 Initializing Demo Data Seeding System...");
seedDemo();
